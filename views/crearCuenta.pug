doctype html
html(lang="en")
    head
        include partials/header
    body.login 
        .contenedor-formulario
            h1 Crea tu cuenta en UpTask

            include partials/mensajes

            form#formulario.caja.caja-login(action="/crear-cuenta" method="POST")
                .campo
                    label(for="email") Email
                    input#email(type="email" name="email" placeholder="Tu Email" value= email)
                .campo
                    label(for="password") Password
                    input#password(type="password" name="password" placeholder="Tu Password" password= password)
                //- .campo
                //-     label(for="confirmar") Confirmar Password
                //-     input#confirmar(type="password" name="confirmar" placeholder="Repite tu Password")
                .campo.enviar
                    input.boton(type="submit" value="Crear Cuenta")
                .campo
                    a(href="/iniciar-sesion") Iniciar Sesión